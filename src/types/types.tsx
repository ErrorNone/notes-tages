export interface INotes {
    id: number;
    title: string;
    body: string;
}

export interface ITages {
    id: number;
    name: string;
}

export interface IOptions {
    value: string;
    label: string;
}

